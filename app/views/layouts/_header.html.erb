<%# <div class="hero-head"> %>
  <header class="navbar is-fixed-top is-dark" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
      <a class="navbar-item" href="#">
        <img src="https://bulma.io/images/bulma-logo.png" width="112" height="28">
      </a>

      <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </a>
    </div>

    <div id="navbarBasicExample" class="navbar-menu">
      <div class="navbar-start">
        <p class="header-link" ><%= link_to ' Home ', root_path %></p>
        <p class="header-link" ><%= link_to ' Guesthouse ', houses_path %></p>
        <p class="header-link" ><%= link_to ' Story ', "#" %></p>
        <p class="header-link" ><%= link_to ' User ', users_path %></p>

        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link">
            More
          </a>

          <div class="navbar-dropdown">

              <p class="header-link" ><%= link_to 'Mypage', user_path(current_user) %></p>

 
              <p class="header-link" ><%= link_to 'Setting', edit_user_path(current_user) %></p>

            <hr class="navbar-divider">
 
              <p class="header-link" ><%= link_to 'About', "#" %></p>
              <p class="header-link" ><%= link_to 'Contact', "#" %></p>

          </div>
        </div>
      </div>

      <div class="navbar-end">
        <div class="navbar-item">
          <div class="buttons">
          <%# if logged_in_admin? %>
            <%#= link_to 'Log out(Admin)', admin_logout_path, method: :delete, class: 'button is-danger' %>
          <% if logged_in? %>
            <%= link_to 'Log out', logout_path, method: :delete, class: 'button is-danger' %>
          <% else %>
            <%= link_to 'Sign Up', signup_path, class: 'button is-primary' %>
            <%= link_to 'Log in', login_path, class: 'button is-light' %>
          <% end %>
          </div>
        </div>
      </div>
    </div>
  </header>
<%# </div> %>
