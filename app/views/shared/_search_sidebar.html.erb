      <aside class="menu">
        <p class="menu-label">
          カテゴリーから探す
        </p>
        <ul class="menu-list">
          <% categories.each do |category| %>
            <li><%= link_to category.name, houses_path(category_id: category.id) %></li>
          <% end %>
            <li><%= link_to "他のカテゴリで探す", categories_path %></li>
        </ul>
        <p class="menu-label">
          エリアで探す
        </p>
        <ul class="menu-list">
        <% house_areas.each do |house_area| %>
          <li><%= link_to house_area.name, houses_path(house_area_id: house_area.id) %></li>
        <% end %>
        </ul>
      </aside>