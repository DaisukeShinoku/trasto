<div>
  <div>
    <h1>管理者ログイン画面</h1>
    <%= form_for(:session, url: admin_login_path) do |f| %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Log in" %>
    <% end %>
  </div>
</div>